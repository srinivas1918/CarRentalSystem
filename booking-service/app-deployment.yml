kind: Service
apiVersion: v1
metadata:
  name: booking-service
spec:
  selector:
    app: booking-service-lable
  ports:
  - protocol: TCP
    port: 8093
    targetPort: 80
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service-deployment
spec:
  selector:
      matchLabels:
        app: booking-service-lable
  replicas: 1
  template:
    metadata:
      labels:
        app: booking-service-lable
    spec:
      containers:
        - name: booking-service-container
          image: gcr.io/invertible-tree-252704/booking-service:latest
          ports:
            - containerPort: 80
